# 数字图像处理综合实验系统 — 使用指南

## 写给初学者

本软件是一个**学习工具**，帮助你理解《数字图像处理》课程中的各种算法。你可以打开一张图片，选择左侧的算法，调节参数，立刻看到处理效果。

**不需要写代码** — 所有操作都通过点击和拖拽完成。右侧会实时显示处理结果和质量指标。

---

## 快速上手

### 第一步：打开图片

点击左上角 **📂 打开图像** 按钮，选择一张 `.jpg` / `.png` / `.bmp` 图片。

左侧显示 **原始图像**，右侧显示 **处理结果**。

### 第二步：选算法

在左侧「算法选择」区，先选 **类别**（如"边缘检测"），再选 **方法**（如"Canny 边缘"）。

下方会显示该方法的文字说明。

### 第三步：调参数 → 看结果

在「参数调节」区调整参数，勾选「参数变化时自动预览」则每次调参立即刷新结果。

也可以点击 **▶ 执行处理** 手动触发。

### 第四步：对比与保存

- 勾选右上角 **叠加对比** → 在结果图上 50% 显示原图，直观看出变化
- 点击 **💾 保存结果** → 导出处理后的图片
- 点击 **↻ 结果作为原图** → 把处理结果变成新的原图，继续下一步操作（流水线处理）

---

## 操作分类详解

### 一、灰度变换与增强

> **学什么？** 改变图像的亮暗、对比度，让看不清的细节显现出来。

| 操作 | 效果 | 什么时候用 |
|------|------|-----------|
| **图像反相** | 黑变白、白变黑，类似照片底片 | 观察负片效果；医学X光片常用 |
| **亮度/对比度** | 调亮调暗，增强或减弱对比 | 照片太暗或太亮时快速修正 |
| **对数变换** | 把很暗的区域提亮，同时不过曝亮区 | 图像暗部看不清细节时 |
| **伽马校正** | gamma<1 提亮暗部，gamma>1 压暗亮部 | 修正显示器亮度曲线；改善曝光 |
| **直方图均衡化** | 自动拉大对比度，让图像更"通透" | 图像发灰发雾时一键改善 |
| **直方图规定化** | 把对比度调整成你想要的样子 | 想把图像调成特定风格（亮调/暗调/均匀） |
| **CLAHE** | 局部区域分别调对比度 | 光照不均匀（一半亮一半暗）的图像 |
| **灰度级分层** | 只保留某个亮度范围的内容，其余变黑 | 提取图像中特定亮度的目标 |
| **位平面分解** | 把8位灰度图拆成8层，每层只有0和1 | 理解图像的数据存储方式；最低位常是噪声 |
| **图像加法去噪** | 多张加噪照片取平均，噪声互相抵消 | 理解"多帧平均降噪"原理 |
| **图像减法差异** | 原图减去模糊图，突出变化/边缘 | 检测图像之间的差异；背景去除 |

> **学习建议**：先从「直方图均衡化」开始，观察直方图从集中变平坦的过程。然后尝试「伽马校正」对比 gamma<1 和 gamma>1 的效果。

---

### 二、空间滤波

> **学什么？** 用一个小窗口（卷积核）在图像上滑动，对每个像素做加权计算。

| 操作 | 效果 | 什么时候用 |
|------|------|-----------|
| **均值滤波** | 取邻居平均值，图像变模糊 | 去除细小的随机噪声（代价是边缘也模糊了） |
| **高斯滤波** | 按距离加权取平均，中心权重大 | 更自然的模糊，是后续边缘检测的标准预处理 |
| **中值滤波** | 取邻居的中位数，不是平均值 | 对付椒盐噪声（黑白斑点）的利器 |
| **双边滤波** | 既考虑距离又考虑颜色相似度 | **保边去噪** — 平滑皮肤但保留轮廓边缘 |
| **拉普拉斯锐化** | 用二阶微分增强边缘 | 让模糊的图片变得更锐利 |
| **反锐化掩蔽** | 原图减去模糊图得到高频，再加回原图 | 比拉普拉斯锐化更可控的锐化方式 |

> **学习建议**：对同一张图分别用均值/高斯/中值滤波，观察去噪效果和边缘保留程度的区别。这是理解"线性"和"非线性"滤波最好的方式。

---

### 三、边缘检测

> **学什么？** 找出图像中亮度突变的位置 — 这通常是物体的边界。

| 操作 | 特点 | 适用场景 |
|------|------|---------|
| **Sobel** | 一阶梯度，对水平/垂直边缘敏感 | 通用边缘检测，起手选择 |
| **Prewitt** | 类似 Sobel 但权重不同 | 对比 Sobel 理解梯度算子差异 |
| **Roberts** | 2×2 交叉梯度，最古老 | 展示早期边缘检测方法 |
| **Kirsch** | 8个方向分别检测，取最大值 | 需要方向信息时；理解"罗盘算子" |
| **Laplacian** | 二阶微分，同时检测上升和下降沿 | 定位边缘的精确位置（零交叉点） |
| **Canny** | 多阶段：平滑→梯度→NMS→双阈值连接 | **效果最好的通用边缘检测**，线条细且连续 |
| **Hough 直线** | 把点映射到参数空间投票 | 检测图像中的直线（车道线/建筑物边缘） |
| **Hough 圆** | 同上但找圆 | 检测圆形物体（硬币/细胞/瞳孔） |
| **SUSAN** | 基于"同值核区"的角点和边缘检测 | 理解不依赖梯度的检测方法 |

> **学习建议**：对同一张图跑 Sobel → Laplacian → Canny，对比三个结果。然后尝试 Hough 直线检测，理解"从边缘点到几何形状"的跨越。

---

### 四、阈值与分割

> **学什么？** 把图像分成"目标"和"背景"两部分，或者把不同物体分离开。

| 操作 | 原理 | 适用场景 |
|------|------|---------|
| **全局阈值(Otsu)** | 自动找最佳阈值，使类间方差最大 | 目标和背景亮度差异明显时 |
| **全局阈值(Triangle)** | 直方图形状法找阈值 | 目标占比小的图像 |
| **手动阈值** | 你自己设定阈值 | 理解阈值概念；调试特定图像 |
| **自适应阈值** | 每个小区域各自算阈值 | 光照不均（文档拍照/反光） |
| **分水岭分割** | 模拟"注水"过程，不同水盆对应不同区域 | 粘连物体的分离（细胞计数/硬币分割） |

> **学习建议**：先用 Otsu 找一个对比度好的图像试试，然后拿光照不均的图对比「自适应阈值」和「全局阈值」的效果差异。

---

### 五、形态学

> **学什么？** 用结构元素（小形状）去"腐蚀""膨胀"图像，改变物体形状。

| 操作 | 效果 | 用途 |
|------|------|------|
| **腐蚀** | 白色区域缩小，黑色区域扩大 | 去噪点、断开粘连 |
| **膨胀** | 白色区域扩大 | 填洞、连接断裂 |
| **开运算** | 先腐蚀再膨胀 | 去小噪点、平滑边界、分开粘连 |
| **闭运算** | 先膨胀再腐蚀 | 填小洞、连接近距离物体 |
| **梯度** | 膨胀减腐蚀 | 提取物体外轮廓 |
| **顶帽** | 原图减开运算 | 提取比背景亮的小结构 |
| **黑帽** | 闭运算减原图 | 提取比背景暗的小结构 |

> **学习建议**：形态学通常在**二值图像**（黑白图）上使用。先做阈值分割得到二值图，再用形态学清理。记住口诀：**开去噪，闭填洞**。

---

### 六、频域处理

> **学什么？** 把图像从"空间"变到"频率"视角 — 图像中的快变化（边缘/噪声）对应高频，慢变化（渐变/平整区域）对应低频。

| 操作 | 效果 | 用途 |
|------|------|------|
| **FFT 低通滤波** | 去掉高频，保留低频 | 平滑去噪（模糊效果） |
| **FFT 高通滤波** | 去掉低频，保留高频 | 提取边缘/细节 |
| **FFT 带通/带阻** | 只保留/去除某段频率 | 去除周期性噪声条纹 |
| **Walsh-Hadamard 变换** | 方波基的变换，纯加减运算 | 理解"非正弦基"的变换；编码理论 |
| **WHT 域滤波** | 在 WHT 域做滤波 | 对比 FFT 和 WHT 的差异 |

> **学习建议**：先理解 FFT 低通/高通的直觉：低频=模糊，高频=边缘。用同一张图对比空域（均值滤波）和频域（低通滤波）的效果 — 它们本质上在做同一件事。

---

### 七、噪声与复原

> **学什么？** 图像在拍摄/传输中会退化（模糊、噪声），想办法恢复本来面目。

| 操作 | 用途 |
|------|------|
| **添加高斯噪声** | 模拟传感器噪声（电子设备常见） |
| **添加椒盐噪声** | 模拟传输错误/坏点（黑白随机斑点） |
| **运动模糊模拟** | 模拟相机/物体移动造成的拖影 |
| **运动模糊复原(维纳)** | 用频域反卷积"反向计算"出原始清晰图像 |
| **维纳自适应滤波** | 局部自适应去噪 — 平坦区多平滑，边缘区少平滑 |

> **学习建议**：先加噪声（高斯/椒盐），再用空间滤波（中值/高斯）尝试去噪。然后做运动模糊→维纳复原的实验，理解"退化+复原"的完整流程。

---

### 八、颜色图像处理

> **学什么？** 图像不仅是亮度，还有颜色信息。

| 操作 | 用途 |
|------|------|
| **B/G/R 通道** | 分别查看蓝/绿/红分量 |
| **Gray** | 转为灰度（亮度通道） |
| **HSV-H/S/V** | 色调/饱和度/明度 — 更符合人的感知 |
| **Lab-L** | Lab 色彩空间的亮度分量 |
| **伪彩色** | 给灰度图赋予彩色，增强人眼分辨力 |
| **白平衡(灰度世界)** | 假设平均颜色是灰色，自动校正偏色 |
| **白平衡(完美反射)** | 假设最亮的点是白色，校正偏色 |

> **学习建议**：打开一张偏暖或偏冷的照片，用白平衡校正，观察 RGB 通道均值的变化。

---

### 九、几何处理

> **学什么？** 改变图像的位置、方向、大小。

| 操作 | 用途 |
|------|------|
| **旋转** | 任意角度旋转 |
| **水平/垂直翻转** | 镜像翻转 |
| **缩放** | 放大或缩小 |

---

### 十、压缩与质量评价

> **学什么？** 理解图像压缩的本质以及如何评价处理结果的好坏。

| 操作/指标 | 含义 |
|-----------|------|
| **JPEG 压缩预览** | 模拟 JPEG 有损压缩 — 质量越低文件越小但细节丢失 |
| **MSE** | 均方误差 — 处理结果与原图像素差的平方均值，越小越好 |
| **PSNR** | 峰值信噪比 — dB为单位，越大越好（>40dB 几乎看不出差异） |
| **SSIM** | 结构相似性 — 0~1，越接近1越相似，比 MSE 更符合人眼感知 |

---

### 十一、小波与多分辨率

> **学什么？** 同时看到图像在不同尺度上的信息 — 既能看整体，又能看细节。

| 操作 | 用途 |
|------|------|
| **小波分解 DWT2** | 把图像拆成 LL(概貌)/LH(水平细节)/HL(垂直细节)/HH(对角细节) |
| **小波阈值去噪** | 对小波系数做软阈值处理，保留大系数（结构），去掉小系数（噪声） |

> **学习建议**：先做一次 DWT2 分解，观察四个子带分别包含什么信息。然后对比小波去噪和空域滤波去噪的效果。

---

### 十二、图像压缩编码

> **学什么？** 如何用更少的比特数表示图像数据。

| 操作 | 用途 |
|------|------|
| **哈夫曼编码统计** | 计算图像信息熵和理论压缩极限 |
| **DPCM 预测编码** | 用前一个像素预测当前像素，只传输"猜错"的误差 |

---

### 十三、图像表示与描述

> **学什么？** 从图像中提取出**数字特征**，让计算机能"认识"物体。

| 操作 | 用途 |
|------|------|
| **边界链码** | 用一串方向数字描述物体轮廓 |
| **GLCM 纹理** | 6个特征（对比度/同质性/能量/熵/相关性/差异性）描述纹理 |
| **距离变换** | 计算每个点到最近边界的距离，形成距离图 |
| **Hu 不变矩** | 7个数字描述形状，**旋转/缩放/平移不变** — 同一个物体不同角度得到的值相同 |
| **傅里叶描述子** | 用少量频域系数重建轮廓 — 系数越多越精确 |

> **学习建议**：打开一张含多个物体的图像，先用 Hu 矩看不同位置的同类型物体是否得到相似的矩值。再用 GLCM 对比粗糙纹理和光滑纹理的特征差异。

---

## 学习路线推荐

### 新手路径（2-3周）

```
第1周: 灰度变换 → 直方图(均衡化/规定化/CLAHE) → 空间滤波
       ↓ 目标：理解像素操作和邻域操作的区别
       
第2周: 边缘检测(Sobel→Canny→Hough) → 阈值分割(Otsu→自适应)
       ↓ 目标：学会从图像中提取边缘和轮廓
       
第3周: 形态学(开/闭/梯度) → 颜色空间 → 噪声与复原
       ↓ 目标：学会清理和增强分割结果
       
进阶:   频域处理 → 小波变换 → 图像描述(Hu矩/GLCM/傅里叶描述子)
        目标：理解频域视角和自动特征提取
```

### 实验建议

1. **同图多法对比** — 对同一张图用不同方法，观察差异
2. **参数扫描** — 固定算法，逐步调参数，观察渐变效果
3. **流水线操作** — 用「结果作为原图」串联多个操作，如：去噪→锐化→边缘检测→Hough直线
4. **质量指标验证** — 做压缩/去噪后，用 MSE/PSNR/SSIM 量化评估
5. **批处理实验** — 对多张图片统一处理，对比不同参数的影响

---

## 界面快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + O` | 打开图像 |
| `Ctrl + S` | 保存结果 |
| `Ctrl + Enter` | 执行处理 |
| `Ctrl + R` | 结果作为原图 |
| `Ctrl + 滚轮` | 缩放图像 |
| `双击图像` | 切换适应窗口 / 原始比例 |

---

## 常见问题

**Q: 为什么我的操作结果是全黑/全白的？**
A: 检查输入图像类型。形态学操作需要二值图（先做阈值分割）；频域滤波对大图效果好，小图不明显。

**Q: MSE/PSNR 一直显示 "-"？**
A: 只有执行处理后才有结果与原图的对比指标。刚打开图片时只有灰度均值和标准差。

**Q: 如何撤销操作？**
A: 点击左侧「执行历史」中的条目可以回溯到该步骤的结果。也可以用「结果作为原图」前保留原始图像。

**Q: 叠加对比怎么用？**
A: 勾选右上角「叠加对比」→ 结果图上半透明叠加原图 → 快速看出哪里发生了变化。
